// ============================================================
// NS TIKTOK COMMAND CENTER — điểm vào dashboard (placeholder Part 4).
// Part 5 sẽ thay bằng ứng dụng React (Tổng quan + Chi tiết kênh).
// ============================================================
import "./styles/tokens.css";

const root = document.getElementById("root");
if (root) {
  root.innerHTML = `
    <div style="min-height:100vh;display:flex;align-items:center;justify-content:center;text-align:center;padding:24px">
      <div>
        <div style="font-family:var(--f-head);letter-spacing:3px;color:var(--gold);font-size:.8rem;margin-bottom:10px">NON SON ANALYTICS</div>
        <h1 style="margin:0 0 8px">NS TikTok Command Center</h1>
        <p style="color:var(--mut)">Dashboard đang được xây dựng — Phase 0 (Part 5).</p>
      </div>
    </div>`;
}
